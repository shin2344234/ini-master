# Community metadata

`.inimeta` files for mods whose authors have not written one. INI Master ships this folder next to `INIMaster.exe` and uses a file here when neither the plugin nor its folder carries one. Name the file after the ini (`OptiScaler.inimeta` for `OptiScaler.ini`). [docs/METADATA.md](../docs/METADATA.md) has the format.
